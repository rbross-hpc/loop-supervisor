"""Placeholder so `pytest` (testpaths = ["tests"], see pyproject.toml)
has something to collect before any real code exists. Delete this once
the project has its own tests -- it asserts nothing about the project
itself."""


def test_placeholder() -> None:
    assert True
